1장 주피터노트북 사용법(
    - shift+enter
    - esc+b, a, m
2장 자료형과 연산자
   1절 변수
      선언 x, 할당과 동시에 타입 설정
      문자, _, 순자 사용가능, 문자로 시작해야 함.
      예약어 사용부가.
      다중 변수 할당 가능(일괄 할당)
    2절 화면 I/O
      input(labeld) : 사용자로부터 문자열을 입력 받는다.
      print(출력내용들, [sep='구분자', end='종결어구'])
    3절 기본자료형
      숫자 : int, float, complex(복소수)
              int/int => 실수
              지수형표현 (ex: 1.12e-20)은 실수
      문자 : +, * 연산
             정규표현식 : re.findall(), re.search()
