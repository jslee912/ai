Ch 10 문서 객체 모델(Document Object Model : DOM)
	; 넓은 의미로 HTML 페이지를 인식하는 방식이고 좁은 의미로 document객체와 관련된 객체의 집합
    10.1 문서 객체 모델과 관련된 용어 정리 ; 태그, 문서객체, 노드
        1)	태그를 자바스크립트에서 이용할 수 있는 객체로 만든 것이 문서 객체.

    10.2 문서 객체 만들기(1) ; 텍스트 노드를 갖는 문서 객체 만들기
        1)	동적으로 문서 객체를 만들기 위해
        ①	Document 객체의 노드 생성 
            var header = document.createElement('h1');
            var textNode = document.createTextNode('Hello DOM');
        ②	문서 객체 연결
            header.appendChild(textNode);
            document.body.appendChild(header);

     10.3 문서 객체 만들기(2) ; 텍스트 노드를 갖지 않는 문서 객체 만들기

     10.4 문서 객체 만들기(3)

     10.5 문서 객체 가져오기(1) ; 이미 존재하는 HTML 태그를 자바스크립트로 가져오는 방법 중 id로 가져오기

     10.6 문서 객체 가져오기(2) ; 태그이름이나 이름으로 가져오기

     10.7 문서 객체 가져오기(3)

     10.8 문서 객체의 스타일 조작

     10.9 문서 객체 제거 

     10.10 문서 객체를 사용한 시계